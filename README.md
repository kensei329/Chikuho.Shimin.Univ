# 筑豊市民大学 ホームページ

福岡県田川市の筑豊市民大学のウェブサイトです。ビルドもサーバー側の処理も要りません。
HTML・CSS・JavaScript を置いてあるだけで、そのまま動きます。

## いちばん大事な決めごと

**スマートフォンの画面だけを作っています。**

本文は幅 520px の縦長の柱を基準に組み、PC で開いたときはその柱を画面の中央に置きます。
デスクトップ向けの別レイアウトはありません。上に固定したヘッダーと、下に固定したタブバーで
行き来する、アプリのような作りです。

会員は70代が中心です。**本文 17px・行間 1.95・タップできる領域は 44px 以上**を守ってください。
ここを縮めると、いちばん読んでほしい人に読めなくなります。

## ページ

| ファイル | 何のページか | 見せ方 |
|---|---|---|
| `index.html` | ホーム | 縦スクロール |
| `katsudo.html` | 活動 | **リール**（上下スワイプで1枚ずつ） |
| `rinen.html` | 理念 | 場面が順に流れるアニメーション |
| `bunkakai.html` | 分科会（四つの会の選び口） | 四つの札だけ |
| `kai-history.html` | 歴史をたどる会 | **ストーリー**（横スワイプ・自動送り） |
| `kai-healthy.html` | ヘルシー | ストーリー |
| `kai-kyoyo.html` | 教養講座 | ストーリー |
| `kai-teizan.html` | 低山 | ストーリー |
| `nenkan.html` | 年間予定 | 縦スクロール（月がぽこっと出る） |

下のタブバーは左から **活動 / 理念 / ホーム（中央） / 分科会 / 年間予定** の5つ。
分科会の4ページを開いているあいだは「分科会」が現在地になります。

## ファイル

| 場所 | 中身 |
|---|---|
| `css/site.css` | 色と寸法の決めごと、ヘッダー、タブバー、共通の部品 |
| `css/page.css` | 縦スクロールのページ（ホーム・理念・分科会の選び口・年間予定） |
| `css/reel.css` | リール |
| `css/story.css` | ストーリー |
| `js/site.js` | 押した手応え、次のページの先読み |
| `js/home.js` | 年会費のその場計算 |
| `js/reel.js` | リールの送り、いいね、解説、シェア |
| `js/story.js` | ストーリーの送り、自動送り、いいね、シェア |
| `js/nenkan.js` | 年間予定の出てくる動き |
| `logo.png` / `logo-mark.png` | ロゴ全体 / 紋章部分（ヘッダーとアイコンに使用） |
| `assets/talk/` | 活動ページの写真 |

**ヘッダーとタブバーは全ページに同じものを書き写しています。**片方だけ直すとずれるので、
直すときは9ファイルすべてに同じ変更を入れてください。

## 色

ロゴから採った三色でできています。

- 深緑 `#033D20` — ヘッダーの文字、タブバー、見出し
- 緑 `#1E6B27` — 添え書き、罫
- オレンジ `#E07A05` / `#F0921C` — 現在地、強調

明るい地の上にオレンジをそのまま置くと読めません。文字として置くときは濃いオレンジ
`#6B3602`（`--gold-ink`）を使ってください。

## 活動ページ（リール）

上下にスワイプして1枚ずつ送る、ショート動画のような見せ方です。カードは5枚。

1. 村田喜代子さん（作家）
2. 中村哲さん（医師）
3. 今井通子さん（登山家・医師）
4. JAXA（宇宙開発）
5. 本年度について

各カードの右側に **いいね・解説・シェア** が並びます。

- **いいね** … このサイトはサーバーを持たないので、押した数を全員で共有することはできません。
  表示しているのは「この端末で押したぶん」だけで、`localStorage` に残ります。
  **他の人の画面には出ません。**もとの数は 0 から始めています。ありもしない数を
  最初から出さないためです
- **解説** … そのカードの `.card__sheet` の中身を下から開きます。件数は出していません
- **シェア** … 端末の共有シートを呼び、出せない環境ではリンクを写します

### 写真の差し替え

`assets/talk/` に、次の名前で置いてください。

| ファイル名 | 誰の写真か | 寸法 |
|---|---|---|
| `01-murata.webp` | 村田喜代子さん | 347×521 |
| `02-nakamura.webp` | 中村哲さん | 676×896 |
| `03-imai.webp` | 今井通子さん | 900×1200 |
| `04-jaxa.webp` | JAXA | 900×1200 |

形式は WebP に揃えています。縦長（3:4 くらい）が収まりよく出ます。
**写真が無くても崩れません。**読み込みに失敗すると `.card__photo` に `is-blank` が付き、
カードごとの色の面に切り替わります。
くわしくは `assets/talk/README.md` を見てください。

### 書いてあることの出どころ

各カードの解説は、公表されている経歴にもとづいて書いています。**講演の日付や、その場で
何を話されたかは書いていません。**記録が手元にないものを書き足さないでください。

## 分科会（ストーリー）

`bunkakai.html` は**四つの札だけ**です。前置きも、あとに続く節も置いていません。
開いてすぐ、どれかを選べる状態にしてあります。

札の中の「ストーリーを見る」は琥珀色で塗ってあります。以前は「見る ›」という小さな文字
でしたが、押せるものだと気づかれませんでした。ロゴの三色にオレンジが入っているので、
深緑の上でも色は浮きません。文字は白ではなく濃い茶を載せています（琥珀の上に白だと
明るすぎて読みにくいため）。

会ごとに1ファイル、3ページ立てです。

1. 開催日と時間
2. どんな会か
3. お申し込みボタン（Googleフォームへ）

5秒で自動的に次へ進みます。押しっぱなしで止まり、上の停止ボタンでも止まります。
動きを減らす設定の環境では、はじめから自動送りをしません。

下の帯には**入力欄を置いていません。**届く先の無い入力欄は、事務局に連絡したつもりに
させてしまうためです。代わりに申し込みフォームへのリンクを置いています。

## 年会費の計算（ホーム）

1講座 10,000円。2講座目からは1講座ごとに 3,000円。4講座すべてで 19,000円が上限です。

| 講座数 | 年会費 |
|---|---|
| 1 | ¥10,000 |
| 2 | ¥13,000 |
| 3 | ¥16,000 |
| 4 | ¥19,000 |

金額を変えるときは `js/home.js` の `FIRST` `MORE` `CAP` を直してください。

## 申し込みフォーム

**Google フォームへ直接飛ばすリンクは、サイト内に1か所だけです。**

サイト中の「お申し込み」「お問い合わせ」は、すべて `index.html#join`（ホームの申し込み欄）
へ通しています。行き先を1か所にまとめておくと、フォームを差し替えるときに直す場所が
増えません。

| 場所 | 行き先 |
|---|---|
| 分科会のストーリー 下の帯「お申し込み・お問い合わせ」（4ページ） | `index.html#join` |
| 分科会のストーリー 最終ページのボタン（4ページ） | `index.html#join` |
| 活動 締めの1枚「入会のお申し込み」 | `index.html#join` |
| ホーム `#join` の埋め込みフォーム | Google フォーム本体 |
| ホーム `#join`「別の画面でフォームを開く」 | Google フォーム本体（控え） |

差し替えるときは、フォームの **送信 > `<>` タブ**に出る `<iframe src="...">` の src を
`index.html` の2か所（埋め込みと控えのリンク）に貼り替えてください。
設問を増減したら、`css/page.css` の `.embed iframe` の高さも合わせてください。

`#` で飛んだときに見出しがヘッダーの下へ潜らないよう、`.page section[id]` に
`scroll-margin-top` を入れてあります。

受け付けはフォームのみです。電話番号とメールアドレスはページに載せていません。

## 設立理念

`rinen.html` は、6つの場面が順に流れるアニメーションです。

1. 筑豊市民大学／設立理念（大きくなりながら現れる）
2. わたしが変われば、周りが変わる（左から右へ流れる）
3. 周りが変われば、地域が変わる（下から上へ立ちのぼる）
4. 地域が変われば、社会が変わる（一点から広がる）
5. 一人の灯が仲間を呼び、まちになり、社会へ広がる情景（線画）
6. 設立理念の全文が、下から上へ 14 秒かけて流れる

場面が進むごとに空の色が変わり、稜線の灯りが一つずつ増えます。画面を押すと次へ。
`←` `→` でも送れ、空白キーで止まります。

終わりの画面には、はじめから見るほかに **活動 / 分科会 / 年間予定** への行き先を置いて
あります。ここで行き止まりにすると、タブバーを押すしか先が無くなるためです。

**最後の一枚だけは、押すと「送る」ではなく「止まる」**ようにしてあります。会員は70代が
中心で、14秒で読み切れないことがあります。送ってしまうと終わりの画面へ行き、本文へ戻る
手立てが「はじめから見る」しか無くなるためです。もう一度押すと流れが再開します。

本文は、団体からお預かりした設立理念の全文です。改行を句読点でつないでいますが、
**言葉は一字も変えていません。**文言を直すときは、原文の意図を必ず確認してから触ってください。

### 直すときの注意

- **ヘッダーとタブバーは、この舞台の外側にあります。**`.frame` は `top: var(--header-h)`
  と `bottom: var(--nav-h)` で留めてあり、間だけを使います。ここを `100dvh` からの
  引き算に書き換えると、iOS でアドレスバーが伸び縮みするたびにずれます
- `--head` / `--foot` は舞台の中の余白です。`--foot` は稜線を座らせる高さも兼ねています
- 場面の秒数は各 `<section>` の `data-dur`、本文が流れる秒数は `js/rinen.js` の `READ_SEC`
- 書体は、サイトがすでに取り寄せているものに合わせてあります。もとの指定に戻すと、
  この1ページのためだけにもう2書体を取りにいきます

## 年間予定

`nenkan.html` に直接書いてあります。特別な回は `<li class="mo mo--special">` にすると
オレンジになります。

いまは 2026年の1月から12月まで。**予定が決まっていない月は、決まっていないと書いています。**
それらしい行事名で埋めないでください。

## 未確定の項目

- 活動ページの写真4枚（`assets/talk/`）
- 公開講演会の次回日程
- 各月の詳しい予定
- 正確な住所と交通案内（会場の地図は大学名で検索したものを埋め込んでいます）
- 分科会の活動写真（いまは色の面のみ）

## ファビコンと、SNSに貼ったときの絵

すべて `logo.png` から書き出しています。作り直すときは、ロゴを差し替えてから同じ切り出し
（`logo.png` の x 70–395 / y 130–305、まわりに10%の余白、地は `#F4F5EE`）でやり直してください。

| ファイル | 何に使うか |
|---|---|
| `favicon.ico` | `/favicon.ico` を直接取りにくる相手（古いブラウザ、一部のクローラ）。16/32/48 の3枚入り |
| `favicon-48.png` `favicon-96.png` `favicon-192.png` | ブラウザのタブと、検索結果 |
| `favicon-512.png` | ホーム画面に置いたとき |
| `apple-touch-icon.png` | iOS のホーム画面。180×180、透過なし（透過だと黒く出る） |
| `site.webmanifest` | 上をまとめて宣言している |
| `og.png` | X・LINE などに貼ったときの絵。1200×630 |

**データURI（base64）で埋め込んではいけません。**Googleはクロールできる実ファイルのURLしか
読まないため、埋め込むと検索結果にアイコンが出ません。

**ファビコンは紋章の一部だけを切り出しています。**ロゴ全体（弧を描く言葉や団体名まで）を
48pxに縮めると、何の絵か分からない塊になります。「開いた本・山・日の出」に絞ってあります。

`og.png` は**中身を真ん中に寄せてあります。**LINEなど正方形に切って出す置き場でも、ロゴが
欠けないためです。透過のまま貼ると黒地で潰れるので、紙色を敷いてあります。X向けに
`twitter:card` は `summary_large_image` にしてあります。

## 公開

Vercel で公開しています（`https://chikuho-shimin-univ.vercel.app/`）。
main ブランチに push すると自動でデプロイされます。それ以外のブランチに push すると、
プレビュー用のURLが作られます。

`google5c7353c4e11c838d.html` は Search Console の所有権確認ファイルです。
**確認が済んだあとも削除しないでください。**消すと所有権が外れます。
`vercel.json` で `cleanUrls` を false にしているのも同じ理由です。true にすると
`/google5c7353c4e11c838d.html` が転送され、確認に失敗します。

**公開URLが変わるとき**は、次を新しいURLに書き換えてください。

1. 各HTMLの `<link rel="canonical">` と `og:url` と `og:image`
2. `index.html` の JSON-LD の `url` と `logo`
3. `robots.txt` と `sitemap.xml`
